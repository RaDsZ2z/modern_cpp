# 1.数组引用
```cpp
#include <iostream>    // std::cout
#include <type_traits> // std::is_same
int main()
{
    int a[3] = {1, 2, 3};
    int(&b)[3] = a;
    using T = int(&)[3];
    std::cout << std::is_same_v<decltype(b), T> << '\n';
    std::cout << (&a == &b);
}
```
